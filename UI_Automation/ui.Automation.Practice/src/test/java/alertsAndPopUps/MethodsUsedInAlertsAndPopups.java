package alertsAndPopUps;

public class MethodsUsedInAlertsAndPopups 
{
	
	// driver().switchTo().alert().accept(); -- It is used to accept the ok button.
	//driver().switchTo().alert().dismiss(); -- It is used to dismiss the alert.
	//driver().switchTo().alert().sendkeys("sss"); -- Used to send data to textbox.
	

}
